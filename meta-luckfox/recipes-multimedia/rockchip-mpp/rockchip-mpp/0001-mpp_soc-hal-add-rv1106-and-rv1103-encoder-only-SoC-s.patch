From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Harry Spencer <harry8@gmail.com>
Date: Fri, 11 Sep 2026 21:00:00 -0700
Subject: [PATCH] mpp: add RV1103/RV1106 support, hardware GIR, CU32, and live stream fixes

Upstream-Status: Pending
---
diff --git a/mpp/codec/enc/h265/h265e_api.c b/mpp/codec/enc/h265/h265e_api.c
index 1637782e3..27cc9d30b 100644
--- a/mpp/codec/enc/h265/h265e_api.c
+++ b/mpp/codec/enc/h265/h265e_api.c
@@ -72,6 +72,8 @@ static MPP_RET h265e_init(void *ctx, EncImplCfg *ctrlCfg)
 
     soc_type = mpp_get_soc_type();
     if (soc_type == ROCKCHIP_SOC_RK3528 ||
+        soc_type == ROCKCHIP_SOC_RV1103 ||
+        soc_type == ROCKCHIP_SOC_RV1106 ||
         soc_type == ROCKCHIP_SOC_RK3576 ||
         soc_type == ROCKCHIP_SOC_RV1126B ||
         soc_type == ROCKCHIP_SOC_RK3572)
diff --git a/mpp/codec/enc/h265/h265e_dpb.c b/mpp/codec/enc/h265/h265e_dpb.c
index b033cb823..ef558495c 100644
--- a/mpp/codec/enc/h265/h265e_dpb.c
+++ b/mpp/codec/enc/h265/h265e_dpb.c
@@ -621,7 +621,7 @@ void h265e_dpb_cpb2rps(H265eDpb *dpb, RK_S32 curPoc, H265eSlice *slice, EncCpbSt
 
         p = h265e_find_cpb_frame(dpb->frame_list, MAX_REFS, frm);
         if (p) {
-            if (!frm->is_lt_ref) {
+            if (1) { /* Always use short-term N-1 references for FPV */
                 p->status.val = frm->val;
                 rps->delta_poc[idx_rps] = p->poc - curPoc;
                 rps->m_used[idx_rps] = 1;
diff --git a/mpp/codec/enc/h265/h265e_header_gen.c b/mpp/codec/enc/h265/h265e_header_gen.c
index 99a2a41d2..d1e1f1a01 100644
--- a/mpp/codec/enc/h265/h265e_header_gen.c
+++ b/mpp/codec/enc/h265/h265e_header_gen.c
@@ -132,7 +132,7 @@ static MPP_RET h265e_write_recovery_point(H265eStream * s, RK_U32 recovery_frame
     h265e_stream_write_se_with_log(s, recovery_frame_cnt, NULL);
     h265e_stream_write1_with_log(s, 1, NULL);
     h265e_stream_write1_with_log(s, 0, NULL);
-    h265e_stream_rbsp_trailing(s);
+    h265e_stream_realign(s);
     return MPP_OK;
 }
 
@@ -183,7 +184,6 @@ static MPP_RET h265e_sei_write(H265eStream *s, RK_U8 uuid[16], const RK_U8 *payl
             h265e_stream_write_with_log(s, uuid[i], 8, NULL);
         for (i = 0; i < data_len; i++)
             h265e_stream_write_with_log(s, (RK_U32) payload[i], 8, NULL);
-        h265e_stream_rbsp_trailing(s);
     } break;
     case H265_SEI_RECOVERY_POINT: {
         h265e_write_recovery_point(s, ((RK_U32 *)payload)[0]);
diff --git a/mpp/codec/enc/h265/h265e_ps.c b/mpp/codec/enc/h265/h265e_ps.c
index 380245d55..37d56dd74 100644
--- a/mpp/codec/enc/h265/h265e_ps.c
+++ b/mpp/codec/enc/h265/h265e_ps.c
@@ -320,12 +320,11 @@ MPP_RET h265e_set_sps(H265eCtx *ctx, H265eSps *sps, H265eVps *vps)
     sps->m_bLongTermRefsPresent = 0;
     sps->m_TMVPFlagsPresent = codec->tmvp_enable;
     sps->m_useStrongIntraSmoothing = codec->cu_cfg.strong_intra_smoothing_enabled_flag;
-    if (cpb_info->max_lt_cnt) {
-        sps->m_numLongTermRefPicSPS = cpb_info->max_lt_cnt;
-        sps->m_bLongTermRefsPresent = 1;
-        sps->m_TMVPFlagsPresent = 0;
-        codec->tmvp_enable = 0;
-    } else if (cpb_info->max_st_tid) {
+    /* Forced 0 for live streaming (eliminates POC 0 grey screen) */
+    cpb_info->max_lt_cnt = 0;
+    sps->m_numLongTermRefPicSPS = 0;
+    sps->m_bLongTermRefsPresent = 0;
+    if (cpb_info->max_st_tid) {
         sps->m_TMVPFlagsPresent = 0;
     }
 
@@ -409,7 +408,7 @@ MPP_RET h265e_set_pps(H265eCtx  *ctx, H265ePps *pps, H265eSps *sps)
     pps->m_PPSId = 0;
     pps->m_SPSId = 0;
     pps->m_picInitQPMinus26 = 0;
-    pps->m_useDQP = 1;
+    pps->m_useDQP = (codec->trans_cfg.diff_cu_qp_delta_depth > 0) ? 1 : 0;
     pps->m_maxCuDQPDepth = codec->trans_cfg.diff_cu_qp_delta_depth;
     pps->m_minCuDQPSize = (sps->m_maxCUSize >> pps->m_maxCuDQPDepth);
 
diff --git a/mpp/hal/rkenc/h264e/hal_h264e_vepu540c.c b/mpp/hal/rkenc/h264e/hal_h264e_vepu540c.c
index 7b91cac37..619920321 100644
--- a/mpp/hal/rkenc/h264e/hal_h264e_vepu540c.c
+++ b/mpp/hal/rkenc/h264e/hal_h264e_vepu540c.c
@@ -1773,6 +1773,8 @@ const MppEncHalApi hal_h264e_vepu540c = {
     .soc_type   = {
         ROCKCHIP_SOC_RK3528,
         ROCKCHIP_SOC_RK3562,
+        ROCKCHIP_SOC_RV1103,
+        ROCKCHIP_SOC_RV1106,
         ROCKCHIP_SOC_BUTT
     },
 };
diff --git a/mpp/hal/rkenc/h265e/hal_h265e_vepu540c.c b/mpp/hal/rkenc/h265e/hal_h265e_vepu540c.c
index fc8d7e283..06c619928 100644
--- a/mpp/hal/rkenc/h265e/hal_h265e_vepu540c.c
+++ b/mpp/hal/rkenc/h265e/hal_h265e_vepu540c.c
@@ -1209,6 +1209,59 @@ static void vepu540c_h265_set_split(H265eV540cRegSet *regs, MppEncCfgSet *enc_cf
     hal_h265e_dbg_func("leave\n");
 }
 
+static MPP_RET setup_vepu540c_intra_refresh(H265eV540cRegSet *regs, H265eV540cHalContext *ctx, RK_U32 refresh_idx)
+{
+    RK_U32 h = ctx->cfg->prep.height;
+    RK_U32 w = ctx->cfg->prep.width;
+    MppEncROICfg roi;
+    MppEncROIRegion region;
+
+    if (!ctx->cfg->rc.refresh_en)
+        return MPP_OK;
+
+    memset(&roi, 0, sizeof(roi));
+    memset(&region, 0, sizeof(region));
+
+    roi.number = 1;
+    roi.regions = &region;
+
+    if (ctx->cfg->rc.refresh_mode == MPP_ENC_RC_INTRA_REFRESH_ROW) {
+        region.x = 0;
+        region.w = w;
+        if (refresh_idx > 0) {
+            region.y = (refresh_idx * 32 * ctx->cfg->rc.refresh_num > 64) ?
+                       (refresh_idx * 32 * ctx->cfg->rc.refresh_num - 64) : 0;
+            region.h = 32 * ctx->cfg->rc.refresh_num + 64;
+        } else {
+            region.y = 0;
+            region.h = 32 * ctx->cfg->rc.refresh_num;
+        }
+        if (region.y + region.h > h)
+            region.h = h - region.y;
+    } else {
+        region.y = 0;
+        region.h = h;
+        if (refresh_idx > 0) {
+            region.x = (refresh_idx * 32 * ctx->cfg->rc.refresh_num > 64) ?
+                       (refresh_idx * 32 * ctx->cfg->rc.refresh_num - 64) : 0;
+            region.w = 32 * ctx->cfg->rc.refresh_num + 64;
+        } else {
+            region.x = 0;
+            region.w = 32 * ctx->cfg->rc.refresh_num;
+        }
+        if (region.x + region.w > w)
+            region.w = w - region.x;
+    }
+
+    region.intra = 1;
+    region.quality = 0;
+    region.abs_qp_en = 0;
+
+    vepu540c_set_roi(&regs->reg_rc_roi.roi_cfg, &roi, w, h);
+
+    return MPP_OK;
+}
+
 MPP_RET hal_h265e_v540c_gen_regs(void *hal, HalEncTask *task)
 {
     H265eV540cHalContext *ctx = (H265eV540cHalContext *)hal;
@@ -1224,8 +1278,8 @@ MPP_RET hal_h265e_v540c_gen_regs(void *hal, HalEncTask *task)
 
     hal_h265e_enter();
     hal_dbg_setup(ctx->dbg_ctx, NULL);
-    pic_width_align8 = (syn->pp.pic_width + 7) & (~7);
-    pic_height_align8 = (syn->pp.pic_height + 7) & (~7);
+    pic_width_align8 = MPP_ALIGN(syn->pp.pic_width, 16);
+    pic_height_align8 = MPP_ALIGN(syn->pp.pic_height, 16);
     pic_wd32 = (syn->pp.pic_width +  31) / 32;
     pic_h32 = (syn->pp.pic_height + 31) / 32;
 
@@ -1280,11 +1334,9 @@ MPP_RET hal_h265e_v540c_gen_regs(void *hal, HalEncTask *task)
     reg_ctl->common.func_en.enc_done_tmvp_en   = 1;
 
     reg_base->common.enc_rsl.pic_wd8_m1    = pic_width_align8 / 8 - 1;
-    reg_base->common.src_fill.pic_wfill    = ((syn->pp.pic_width & 0x7) != 0)
-                                             ? (8 - (syn->pp.pic_width & 0x7)) : 0;
+    reg_base->common.src_fill.pic_wfill    = pic_width_align8 - syn->pp.pic_width;
     reg_base->common.enc_rsl.pic_hd8_m1    = pic_height_align8 / 8 - 1;
-    reg_base->common.src_fill.pic_hfill    = ((syn->pp.pic_height & 0x7) != 0)
-                                             ? (8 - (syn->pp.pic_height & 0x7)) : 0;
+    reg_base->common.src_fill.pic_hfill    = pic_height_align8 - syn->pp.pic_height;
 
     reg_base->common.enc_pic.enc_stnd      = 1; //H265
     reg_base->common.enc_pic.cur_frm_ref   = !syn->sp.non_reference_flag; //current frame will be refered
@@ -1334,6 +1386,12 @@ MPP_RET hal_h265e_v540c_gen_regs(void *hal, HalEncTask *task)
     if (ctx->roi_data)
         vepu540c_set_roi(&regs->reg_rc_roi.roi_cfg, ctx->roi_data,
                          ctx->cfg->prep.width, ctx->cfg->prep.height);
+
+    {
+        EncFrmStatus *frm_status = &task->rc_task->frm;
+        if (frm_status->is_i_refresh)
+            setup_vepu540c_intra_refresh(regs, ctx, frm_status->seq_idx % ctx->cfg->rc.gop);
+    }
     /*paramet cfg*/
     vepu540c_h265_global_cfg_set(ctx, regs);
 
@@ -1682,6 +1740,8 @@ const MppEncHalApi hal_h265e_vepu540c = {
     .client     = VPU_CLIENT_RKVENC,
     .soc_type   = {
         ROCKCHIP_SOC_RK3528,
+        ROCKCHIP_SOC_RV1103,
+        ROCKCHIP_SOC_RV1106,
         ROCKCHIP_SOC_BUTT
     },
 };
diff --git a/mpp/hal/rkenc/jpege/hal_jpege_vepu540c.c b/mpp/hal/rkenc/jpege/hal_jpege_vepu540c.c
index e57a83578..6138aa7a1 100644
--- a/mpp/hal/rkenc/jpege/hal_jpege_vepu540c.c
+++ b/mpp/hal/rkenc/jpege/hal_jpege_vepu540c.c
@@ -446,6 +446,8 @@ const MppEncHalApi hal_jpege_vepu540c = {
     .client     = VPU_CLIENT_RKVENC,
     .soc_type   = {
         ROCKCHIP_SOC_RK3528,
+        ROCKCHIP_SOC_RV1103,
+        ROCKCHIP_SOC_RV1106,
         ROCKCHIP_SOC_RK3562,
         ROCKCHIP_SOC_BUTT
     },
diff --git a/osal/inc/mpp_soc.h b/osal/inc/mpp_soc.h
index 218a454a8..e5ecbd8ea 100644
--- a/osal/inc/mpp_soc.h
+++ b/osal/inc/mpp_soc.h
@@ -53,6 +53,8 @@ typedef enum RockchipSocType_e {
     ROCKCHIP_SOC_RK3572,
     ROCKCHIP_SOC_RK3539,
     ROCKCHIP_SOC_RK182X,
+    ROCKCHIP_SOC_RV1103,
+    ROCKCHIP_SOC_RV1106,
     ROCKCHIP_SOC_BUTT,
 } RockchipSocType;
 
diff --git a/osal/mpp_soc.c b/osal/mpp_soc.c
index c146e5570..9156788aa 100644
--- a/osal/mpp_soc.c
+++ b/osal/mpp_soc.c
@@ -1102,6 +1102,30 @@ static const MppSocInfo mpp_soc_infos[] = {
         {   &rkjpegd, NULL, NULL, NULL, NULL, NULL},
         {   &rkjpege_vpu720, NULL, NULL, NULL},
     },
+    {   /*
+        * rv1103 has codec:
+        * 1 - RK H.264/H.265/MJPEG encoder (vepu540c)
+        * Encoder-only IPC SoC; no hardware decoder.
+        */
+        "rv1103",
+        ROCKCHIP_SOC_RV1103,
+        HAVE_RKVENC,
+        {   NULL, NULL, NULL, NULL, NULL, NULL},
+        {   &vepu540c, NULL, NULL, NULL},
+    },
+    {   /*
+        * rv1106 has codec:
+        * 1 - RK H.264/H.265/MJPEG encoder (vepu540c)
+        * Encoder-only IPC SoC; no hardware decoder.
+        * Listed after rv1103 so strstr() reverse scan selects rv1106
+        * when device-tree compatible contains both rv1103g and rv1106g3.
+        */
+        "rv1106",
+        ROCKCHIP_SOC_RV1106,
+        HAVE_RKVENC,
+        {   NULL, NULL, NULL, NULL, NULL, NULL},
+        {   &vepu540c, NULL, NULL, NULL},
+    },
 };
 
 static const MppSocInfo mpp_soc_default = {
